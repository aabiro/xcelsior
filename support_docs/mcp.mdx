---
title: Xcelsior MCP Server
subtitle: The missing backend for agentic AI.
slug: mcp-server
---

# Xcelsior MCP Server

Xcelsior provides a **Model Context Protocol (MCP)** server that allows AI agents to directly control real GPUs. It turns natural language into compute — instantly, securely, and globally.

Instead of writing custom API scripts, you can connect your AI agent (like Cursor, Claude Desktop, or a custom application) directly to Xcelsior.

## Getting Started

There are two ways to connect, and both expose the same tools. Pick based on whether your agent can reach a remote server.

### Option 1 — Hosted server (recommended)

The Xcelsior MCP Server is a hosted service at `https://xcelsior.ca/mcp`, using **Streamable HTTP** transport.

1. Go to your **Dashboard → Settings → MCP Agent Setup**.
2. Click **Create MCP client**. This generates an OAuth client scoped for agent workflows (for example `instances:read`, `instances:write`, `billing:read`).
3. Copy the URL and bearer token into your agent's configuration, such as `cursor.json` or `claude_desktop_config.json`.

The fastest route is **Dashboard → MCP (Quick Connect)**, which gives you a ready-made prompt with your access token already embedded — paste it to your agent and it configures itself.

### Option 2 — Agent Skill (no MCP client needed)

Some agents have no MCP support at all. For those, install the Xcelsior Agent Skill, which teaches the agent to drive the platform through the official TypeScript SDK:

```bash
npx skills add xcelsior-gpu/skill
```

Then authenticate by exporting the same access token from Quick Connect:

```bash
export XCELSIOR_ACCESS_TOKEN="xcl_..."
```

The agent installs `@xcelsior-gpu/sdk` and can then list GPUs, check spot pricing, estimate cost, launch and monitor instances, and check your balance — in natural language, without speaking the Model Context Protocol at all.

This is the command-line route: you install once from your shell, and any agent on that machine picks it up. Because it goes through the generated SDK rather than hand-written requests, the agent gets typed responses and real error classes, and cannot drift from the API's wire format.

The skill is open source at [github.com/xcelsior-gpu/skill](https://github.com/xcelsior-gpu/skill).

## Agent Tool Capabilities

We expose a curated surface of high-level tools designed specifically for AI agents. The tools fall into these groups:

### 1. Discovery (Read-only)
- `list_available_gpus`: Search the active fleet.
- `get_spot_prices`: Check the live dynamic spot pricing.
- `search_marketplace`: Filter GPU hosts.

### 2. Compute Lifecycle
- `create_instance`: Deploy jobs via containers, including git repositories and initialization scripts.
- `cancel_instance` / `terminate_instance`
- `get_instance_logs`

### 3. Serverless Inference
- `create_serverless_endpoint`
- `run_serverless_job`
- `get_serverless_job_status`

### 4. Billing & Guardrails
- `get_wallet_balance`
- `estimate_job_cost`
- **`should_i_run_this`**: A composite tool agents use to evaluate cost, wallet sufficiency, and jurisdictional rules *before* running a workload.

### 5. Composite Workflows
- `run_training_job`: Instructs the agent to spin up an instance, wait for it to boot, and stream the logs back.
- `schedule_under_budget`: Tells the agent to wait and poll spot prices until a specific GPU model drops below your specified CAD/hour max budget.

## Security & Guardrails

Agents are powerful, so we enforce strict guardrails:
- **No Master Tokens:** The MCP server rejects the `XCELSIOR_API_TOKEN` master admin token. Agents authenticate via scoped OAuth Client Credentials.
- **Confirmation Gates:** Destructive or billable tools (`create_instance`, `terminate_instance`) have a `confirm` schema field. If an agent tries to run them with `confirm: false`, the server safely returns a dry-run cost estimate without billing you.
- **OAuth Scopes:** The MCP layer strictly validates scopes (`gpu:read`, `billing:read`) before it proxies requests to the core backend.
