FROM python:3.11-slim

# Install PyTorch and ML libraries with CUDA support
RUN pip install torch torchvision transformers accelerate --index-url https://download.pytorch.org/whl/cu118

# Set working directory
WORKDIR /app

# Copy inference script
COPY inference.py /app/

# Run inference
CMD ["python", "inference.py"]
