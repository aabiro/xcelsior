# Copy to .env.audit (gitignored) or export before MCP / Playwright dashboard audits.
# Provision on production:
#   bash scripts/provision_audit_user.sh
# Local test:
#   XCELSIOR_ENV=test python scripts/ensure_audit_user.py --provision --write-env --verify-login

AUDIT_BASE=https://xcelsior.ca
AUDIT_EMAIL=site-audit@xcelsior.ca
# Set manually or let ensure_audit_user.py generate on --provision:
AUDIT_PASSWORD=