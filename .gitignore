# Agent signing private key (public key + .sig files are committed)
scripts/agent-signing.key

# ── Environment ───────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.audit.example
!.env.paypal-sandbox.example
!.env.paypal-live.example
!.env.stripe-live.example

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.pytest_cache/
htmlcov/
.coverage
*.egg-info/
dist/
build/

# ── Virtual environments ─────────────────────────────────────────────
venv/
.venv/

# ── Logs ──────────────────────────────────────────────────────────────
*.log

# ── Databases (SQLite) ───────────────────────────────────────────────
*.db
*.db-shm
*.db-wal
xcelsior.db*
xcelsior_events.db*
xcelsior_billing.db*
xcelsior_reputation.db*
xcelsior_privacy.db*
xcelsior_sla.db*
xcelsior_stripe.db*

# ── Runtime data (legacy JSON files) ─────────────────────────────────
hosts.json
jobs.json
billing.json
marketplace.json
autoscale_pool.json

# ── Node / Frontend ───────────────────────────────────────────────────
node_modules/
.next/
out/

# ── Builds / artifacts ───────────────────────────────────────────────
builds/
/artifacts/
uploads/

# ── Docker ────────────────────────────────────────────────────────────
docker-compose.override.yml

# ── HPC / Slurm artifacts (generated; not source) ─────────
# NOTE: do NOT use bare slurm*.py / test_slurm*.py here — those globs
# swallow the slurm_adapter.py source module and its tests. Only ignore
# Slurm-generated runtime artifacts.
slurm/
nibi/
*.sbatch
*.slurm
slurm-*.out
docs/competitive_edge.md
docs_phase_plan.md
prompt.md
plan.md

# ── Reports (generated, not tracked) ─────────────────────────────────
docs/*
!docs/site-audit-prompt.md
!docs/site-audit-report-2026-06-04.md
!docs/site-audit-report-2026-06-04-reaudit.md
!docs/site-audit-report-2026-06-04-reaudit-mcp.md
!docs/site-audit-report-2026-06-06-post-recovery.md
!docs/VOLUMES_RUNBOOK.md
!docs/VOLUMES_COMPLETION_PLAN.md
!docs/NEXT_PRIORITIES_ROADMAP.md
!docs/INFERENCE_DESIGN.md
!docs/site-audit-report-2026-06-08-post-recovery.md
!docs/SPOT_INSTANCE_INTEGRATION_PLAN.md

# ── Editor / OS ──────────────────────────────────────────────────────
.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Backups (temporary) ──────────────────────────────────────────────
*.bak

# ── Secrets ───────────────────────────────────────────────────────────
*.pem
*.key
id_rsa*

# SVG generation scripts
scripts/calc_cities.py
scripts/canada_svg_generator.py
scripts/gen_alaska_path.py
scripts/globe_transform.py

frontend/public/sw*
frontend/playwright-report/
frontend/test-results/

# ── Checkpoints (test artifacts) ─────────────────────────────────────────
checkpoints/
.dev-logs/

# ── AI coding agent configs (local-only) ─────────────────────────────────
.adal/
.agents/
.augment/
.bob/
.claude/skills/
.claude/._*
.codebuddy/
.codex
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mcp.json
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
skills/
skills-lock.json

# ── macOS resource forks ─────────────────────────────────────────────────
._*

# ── CI artifacts (local schema cache mirror) ─────────────────────────────
ci-cache/

# ── Stray files ──────────────────────────────────────────────────────────
tatus\ --short
et
wget-log
.hypothesis/

# Next.js incremental build cache
frontend/tsconfig.tsbuildinfo
.npmrc
.env.local
