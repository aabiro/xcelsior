# Endpoint inventory (generated)

Generated by `scripts/generate_endpoint_inventory.py`. **Do not edit by hand** — regenerate it. GT0 step 1 of [mcp-tool-surface-plan.md](../mcp-tool-surface-plan.md).

**519 operations across 36 modules.**

**Auth column:** this codebase authenticates inside handler bodies rather than via FastAPI `Depends`, so the guard is recovered from the handler source. `_get_current_user` resolves an *optional* principal and is not a guard. `none found` means exactly that — verify by hand before concluding an endpoint is public.

**519 of 519 operations classified.** `class` is `covered` / `gap` / `internal` / `redundant`, each with a reason, and GT0 closes only when zero rows are unclassified. Edit [endpoint-classification.json](../endpoint-classification.json), not this table — this file is regenerated and would discard the labels.

## `routes/action_plans.py` — 6 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/v1/launch-plans` | _require_scope | §14.1 preview. Creates an action plan; no attempt/allocation/lease/hold/job. | covered | MCP server calls this path |
| GET | `/api/v1/launch-plans/{plan_id}` | _require_scope | Return action status only to its tenant; foreign ids remain not-found. | covered | MCP server calls this path |
| POST | `/api/v1/launch-plans/{plan_id}/approve` | _require_scope | §14.2 approval. Standing policy self-approves inside limits; else a human. | gap | approving and revoking a launch plan. P4 turns the approval gate into leverage — one approval covering a dependency graph — and no tool approves anything today; get_mcp_action_status only reads |
| POST | `/api/v1/launch-plans/{plan_id}/execute` | _require_scope | §14.3 execute. Exactly-once; a price move beyond tolerance is 409 quote_changed. | covered | MCP server calls this path |
| POST | `/api/v1/launch-plans/{plan_id}/revoke` | _require_scope | §14.2 revoke — idempotent; a consumed or terminal plan cannot be revoked. | gap | approving and revoking a launch plan. P4 turns the approval gate into leverage — one approval covering a dependency graph — and no tool approves anything today; get_mcp_action_status only reads |
| POST | `/api/v1/placements/simulate` | _require_scope | §18 placement feasibility for a spec — read-only. | covered | MCP server calls this path |

## `routes/admin.py` — 26 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/admin/activity` | _require_admin | Admin activity feed — recent events, job stats by day, events by type. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/agent-tokens/coverage` | _require_admin | How far field-wide rotation has progressed across the fleet. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/agent/rollout` | _require_admin | Enqueue an ``upgrade_agent`` directive across fleet (rolling batch). | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/ai-conversations` | _require_admin | Paginated AI conversation list with messages for admin review. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/ai-stats` | _require_admin | Aggregate AI conversation statistics for the admin dashboard. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/control-plane/jobs` | _require_admin | List jobs with their transactional active attempts, lease states, and fencing tokens. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/control-plane/scheduled-tasks` | _require_admin | List durable scheduled tasks from database. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/hosts/{host_id}/agent-tokens` | _require_admin | Credential history for one host. Secrets are never returned. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/hosts/{host_id}/agent-tokens` | _require_admin | Issue (or rotate) this host's agent token. Returns the secret once. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/hosts/{host_id}/agent-tokens/revoke` | _require_admin | Revoke every live token for a host — immediate, no grace window. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/infrastructure` | _require_admin | Admin infrastructure view — hosts by state, GPU model, province, verification, reputation. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/overview` | _require_admin | Admin overview with KPIs and trend data for a configurable window. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/reconciler/findings` | _require_admin | Get reconciler findings, filtered by status ('open', 'resolved', 'all'). | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/reconciler/findings/{finding_id}/dismiss` | _require_admin | Manually dismiss/resolve a finding without enforcing remediation. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/reconciler/findings/{finding_id}/enforce` | _require_admin | Manually enforce remediation for a specific finding. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/reconciler/reconcile-host/{host_id}` | _require_admin | Manually trigger a reconciliation pass on a specific host. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/revenue` | _require_admin | Admin revenue breakdown — by day, GPU model, province, top customers/providers. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/stats` | _require_admin | Get admin dashboard statistics. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/teams` | _require_admin | List all teams with their members. Platform admin only. | internal | platform admin console; operator surface, not the tool surface |
| DELETE | `/api/admin/teams/{team_id}/members/{email}` | _require_admin | Remove a member from any team. Platform admin override. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/unit-economics` | _require_admin | Phase-0 truth: are we making money (margin) and where do users drop off (funnel). | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/users` | _require_admin | List all users for admin panel. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/users/{email}/role` | _require_admin | Admin sets a user's role. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/users/{email}/toggle-admin` | _require_admin | Admin toggles another user's admin flag. | internal | platform admin console; operator surface, not the tool surface |
| GET | `/api/admin/verification-queue` | _require_admin | Get verification queue for admin panel. | internal | platform admin console; operator surface, not the tool surface |
| POST | `/api/admin/web-push/test-notification` | _require_admin | Send a real web-push smoke notification to the current admin user. | internal | platform admin console; operator surface, not the tool surface |

## `routes/agent.py` — 18 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/agent/benchmark` | _require_agent_auth | Receive compute benchmark results from an agent. | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/agent/commands/{host_id}` | _require_agent_auth | Drain pending agent commands for this host. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/degraded` | _require_agent_auth | Receive degraded-state reports from worker hot loops (B11). | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/lease/claim` | _require_agent_auth | Agent claims a lease for an assigned job. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/lease/release` | _require_agent_auth | Agent releases its lease (job completed/failed/preempted). | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/lease/renew` | _require_agent_auth | Agent renews its lease on a job. Must be called before expiry. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/logs/{job_id}` | _require_agent_auth | Ingest container log lines from the worker agent. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/mining-alert` | _require_agent_auth | Receive mining detection alert from an agent. | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/agent/popular-images` | _require_agent_auth | Return popular container images for agent pre-pulling. | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/agent/preempt/{host_id}` | _require_agent_auth | Check if any jobs on this host should be preempted. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/preempt/{host_id}/{job_id}` | none found — verify by hand | Schedule a job for preemption on a host. Admin-only. | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/agent/ssh-keys/{job_id}` | _require_agent_auth | Return the job owner's SSH public keys so the agent can inject them into containers. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/ssh-status/{job_id}` | _require_agent_auth | Worker reports the final state of SSH setup for a job. | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/telemetry` | _require_agent_auth | Receive periodic GPU telemetry from agent (every 5s). | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/agent/telemetry/{host_id}` | none found — verify by hand | Get latest telemetry for a host (dashboard live gauges). | internal | worker-agent callbacks, authenticated by the fleet secret |
| POST | `/agent/versions` | none found — verify by hand | Evaluate self-reported versions without mutating host admission. | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/agent/work/{host_id}` | _require_agent_auth | Pull pending work for an agent. Returns assigned jobs. | internal | worker-agent callbacks, authenticated by the fleet secret |
| GET | `/api/telemetry/all` | none found — verify by hand | Get latest telemetry for all hosts (dashboard overview). | internal | worker-agent callbacks, authenticated by the fleet secret |

## `routes/agent_v2.py` — 10 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/agent/v2/attempts/status` | _require_agent_auth | Fenced worker status report. Terminal reports settle allocations, | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/commands/claim` | _require_agent_auth | Claim attempt-bound commands (pending → claimed; redelivered on | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/commands/{command_id}/ack` | _require_agent_auth | Once-only terminal ACK; duplicates replay the stored result. | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/commands/{command_id}/nack` | _require_agent_auth | Typed failure report → bounded-backoff redelivery or dead-letter. | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/leases/claim` | _require_agent_auth | THE hard gate: no 200 here → no container start, ever. | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/leases/release` | _require_agent_auth | Voluntary release by the current authority holder; idempotent. | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/leases/renew` | _require_agent_auth | CAS renewal under the exact authority tuple. A 409 means the | internal | worker-agent v2 callbacks; not a user surface |
| GET | `/agent/v2/negotiate/{host_id}` | _require_agent_auth | Which protocol should this host speak? (canary rollout control) | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/observations` | _require_agent_auth | Ingest one full-state worker observation (immutable snapshot); | internal | worker-agent v2 callbacks; not a user surface |
| POST | `/agent/v2/tokens/rotate` | none found — verify by hand | Exchange this host's current agent token for a fresh one. | internal | worker-agent v2 callbacks; not a user surface |

## `routes/artifacts.py` — 6 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/artifacts` | _require_scope, _get_current_user | List artifacts visible to the caller: their jobs' outputs plus standalone uploads. | gap | P3 needs artifact read and promotion; no tool exposes them |
| POST | `/api/artifacts/download` | _require_scope, _get_current_user | Get a presigned download URL for an artifact. | gap | P3 needs artifact read and promotion; no tool exposes them |
| POST | `/api/artifacts/finalize` | _require_scope, _get_current_user | Finalize an upload session, performing validation and transitioning status. | gap | P3 needs artifact read and promotion; no tool exposes them |
| POST | `/api/artifacts/upload` | _require_scope, _get_current_user | Get a presigned upload URL for an artifact. | gap | P3 needs artifact read and promotion; no tool exposes them |
| GET | `/api/artifacts/{job_id}` | _require_scope, _get_current_user | List all artifacts for a job. | gap | P3 needs artifact read and promotion; no tool exposes them |
| GET | `/api/artifacts/{job_id}/expiry` | _require_scope, _get_current_user | Get expiry/cleanup dates for artifacts of a given job. | gap | P3 needs artifact read and promotion; no tool exposes them |

## `routes/auth.py` — 52 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/.well-known/jwks.json` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| GET | `/.well-known/oauth-authorization-server` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| GET | `/api/agent-keys` | none found — verify by hand | List the caller's agent keys for the Settings management view. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| DELETE | `/api/agent-keys/{key_id}` | none found — verify by hand | Revoke a key immediately. Scoped to the owner. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| PATCH | `/api/agent-keys/{key_id}` | none found — verify by hand | Rename a key so several are tellable apart in Settings. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| POST | `/api/auth/change-password` | none found — verify by hand | Change password for the authenticated user. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/auth/demo-credentials` | none found — verify by hand | Reveal the standing demo-account credentials, but ONLY to whitelisted IPs | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/device` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| GET | `/api/auth/introspect` | _get_current_user | Validate any bearer token — interactive user OR machine/client_credentials — | covered | MCP server calls this path |
| POST | `/api/auth/login` | none found — verify by hand | Authenticate with email and password. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/logout` | _get_current_user | Logout — invalidate session and clear cookie. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| DELETE | `/api/auth/me` | none found — verify by hand | Start the durable cross-store account deletion workflow. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/auth/me` | _get_current_user | Get the currently authenticated user's profile. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| PATCH | `/api/auth/me` | none found — verify by hand | Update the current user's profile fields. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| DELETE | `/api/auth/me/avatar` | none found — verify by hand | Remove the user's profile photo. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/auth/me/avatar` | none found — verify by hand | Serve the authenticated user's profile photo. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/me/avatar` | none found — verify by hand | Upload a profile photo (JPEG, PNG, or WebP, max 2 MB). | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/auth/me/data-export` | none found — verify by hand | Export all personal data for the current user. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/me/email-change` | none found — verify by hand | Start a verified email change: emails a confirmation link to the NEW | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/me/email-change/confirm` | none found — verify by hand | Confirm a pending email change via the token from the verification email. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| PUT | `/api/auth/me/profile` | none found — verify by hand | Update the signed-in user's display name (and optional locale fields). | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/oauth/facebook/deauthorize` | none found — verify by hand | Facebook Deauthorize Callback. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/oauth/facebook/delete-data` | none found — verify by hand | Facebook Data Deletion Callback. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/oauth/{provider}` | none found — verify by hand | Initiate OAuth flow — returns the provider's authorization URL. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/auth/oauth/{provider}/callback` | none found — verify by hand | OAuth callback — exchanges authorization code for user profile, creates session, | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/password-reset` | none found — verify by hand | Initiate a password reset. Sends email with reset link. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/password-reset/confirm` | none found — verify by hand | Confirm password reset with token and set new password. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/refresh` | _get_current_user | Refresh an existing session token. | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| POST | `/api/auth/register` | none found — verify by hand | Register a new user with email and password. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/resend-verification` | none found — verify by hand | Resend the email verification link. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/auth/sessions` | none found — verify by hand | List active sessions for the current user. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| DELETE | `/api/auth/sessions/{token_prefix}` | none found — verify by hand | Revoke a specific session by its token prefix. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/token` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| GET | `/api/auth/verify` | none found — verify by hand | — | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/verify` | none found — verify by hand | — | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| POST | `/api/auth/verify-email` | none found — verify by hand | Verify a user's email address with the token sent during registration. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/api/keys` | none found — verify by hand | API keys have been permanently disabled. Use OAuth 2.0 client_credentials. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| POST | `/api/keys/generate` | none found — verify by hand | API keys have been permanently disabled. Use OAuth 2.0 client_credentials. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| DELETE | `/api/keys/{key_preview}` | none found — verify by hand | API keys have been permanently disabled. Use OAuth 2.0 client_credentials. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| GET | `/api/mcp/quick-connect` | none found — verify by hand | Powers the /dashboard/mcp page: always returns a live, ready-to-paste | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| GET | `/api/oauth/clients` | none found — verify by hand | — | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| POST | `/api/oauth/clients` | none found — verify by hand | — | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| DELETE | `/api/oauth/clients/{client_id}` | none found — verify by hand | — | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| PATCH | `/api/oauth/clients/{client_id}` | none found — verify by hand | Update an OAuth client's metadata (name, redirect URIs, scopes, status). | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| POST | `/api/oauth/clients/{client_id}/rotate-secret` | none found — verify by hand | Rotate an OAuth client's secret. Returns the new plaintext secret once. | internal | issues or revokes credentials (agent keys, API keys, OAuth clients, Quick Connect). A tool that mints its own credential can widen its own scopes |
| GET | `/api/users/me/preferences` | none found — verify by hand | Get user preferences. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| PUT | `/api/users/me/preferences` | none found — verify by hand | Update user preferences. | internal | first-party account and session management — login, password, email, profile, sessions; no P0-P7 journey changes someone's credentials |
| GET | `/oauth/authorize` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| POST | `/oauth/authorize` | _get_current_user | Record the user's decision on a staged authorization request. | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| POST | `/oauth/device/authorize` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| POST | `/oauth/register` | none found — verify by hand | RFC 7591 dynamic client registration — the compatibility path. | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |
| POST | `/oauth/token` | none found — verify by hand | — | internal | OAuth/OIDC protocol surface — the authorization server itself. An MCP client speaks this to obtain a token; a tool never calls it |

## `routes/autoscale.py` — 6 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/autoscale/cycle` | _require_scope, _require_admin | Run a full autoscale cycle: scale up, process queue, scale down. | internal | scheduler-driven capacity control, no caller outside the platform |
| POST | `/autoscale/down` | _require_scope, _require_admin | Scale down: deprovision idle autoscaled hosts. | internal | scheduler-driven capacity control, no caller outside the platform |
| GET | `/autoscale/pool` | _require_scope, _require_admin | List the autoscale pool. | internal | scheduler-driven capacity control, no caller outside the platform |
| POST | `/autoscale/pool` | _require_scope, _require_admin | Add a host to the autoscale pool. | internal | scheduler-driven capacity control, no caller outside the platform |
| DELETE | `/autoscale/pool/{host_id}` | _require_scope, _require_admin | Remove a host from the autoscale pool. | internal | scheduler-driven capacity control, no caller outside the platform |
| POST | `/autoscale/up` | _require_scope, _require_admin | Scale up: provision hosts for queued jobs. | internal | scheduler-driven capacity control, no caller outside the platform |

## `routes/billing.py` — 51 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/analytics/enhanced` | _get_current_user | Enhanced analytics endpoint for the rich analytics dashboard. | internal | Feeds the dashboard's rich analytics view — its summary says so. Shaped for charts rather than for an answer, and the underlying numbers are reachable through the billing and usage rows classified above. |
| GET | `/api/analytics/usage` | _require_scope, _require_auth | Usage analytics for both providers and submitters. | gap | Usage analytics across both sides of the marketplace. No tool reads it, and the provider half belongs to the axis that has zero tools today. |
| GET | `/api/billing/attestation` | none found — verify by hand | Get the Xcelsior supplier attestation bundle for procurement review. | internal | operator, testing and compliance surface — refunds, the billing sweep, test-state reset, the attestation document; not a tenant capability |
| POST | `/api/billing/crypto/deposit` | _require_scope | Create a BTC deposit request. Returns address, amount, and QR data. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/crypto/deposit/{deposit_id}` | none found — verify by hand | Poll deposit confirmation status. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/crypto/enabled` | none found — verify by hand | Check if Bitcoin deposits are enabled. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/crypto/rate` | none found — verify by hand | Get current BTC/CAD exchange rate. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| POST | `/api/billing/crypto/refresh/{deposit_id}` | none found — verify by hand | Refresh an expired deposit with a new BTC/CAD rate. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| POST | `/api/billing/free-credits/{customer_id}` | _get_current_user | Claim one-time $10 CAD signup bonus. | internal | one-time signup bonus, claimed once per customer from the dashboard; not a funding rail an agent drives |
| GET | `/api/billing/free-credits/{customer_id}/status` | _get_current_user | Check whether the customer has already claimed the free signup bonus. | internal | one-time signup bonus, claimed once per customer from the dashboard; not a funding rail an agent drives |
| GET | `/api/billing/invoice/{customer_id}` | none found — verify by hand | Generate an invoice for a billing period, itemised by job. | gap | Generates an itemised invoice for a billing period. list_invoices covers the plural /api/billing/invoices/{cid}, which enumerates invoices; nothing produces the line items behind one, which is the answer to 'what exactly did I pay for'. |
| GET | `/api/billing/invoice/{customer_id}/download` | none found — verify by hand | Download an invoice as CSV or plain-text PDF-style document. | redundant | The same invoice as the row above, serialised as CSV or plain text for a browser download — the precedent already set for /instances/{job_id}/logs/download. The itemisation is the gap to close; this rendering of it is not a second one. |
| GET | `/api/billing/invoices/{customer_id}` | none found — verify by hand | List past invoices for a customer (monthly summaries). | covered | MCP server calls this path |
| POST | `/api/billing/lightning/deposit` | _require_scope | Create a Lightning invoice for depositing CAD credits. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/lightning/deposit/{deposit_id}` | _require_scope, _require_auth | Check the status of a Lightning deposit. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/lightning/enabled` | none found — verify by hand | Check if Lightning deposits are enabled and node is reachable. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/lightning/rate` | none found — verify by hand | Get current BTC/CAD rate for Lightning deposits. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| POST | `/api/billing/payment-intent` | _require_scope | Create a Stripe PaymentIntent for depositing compute credits. | internal | card and subscription management. The plan puts adding a card out of the agent surface entirely: card data belongs on a first-party page and the agent only ever references what is already on file |
| GET | `/api/billing/payment-methods` | _require_scope, _get_current_user | List the customer's saved card payment methods. | covered | list_payment_methods calls this path |
| DELETE | `/api/billing/payment-methods/{payment_method_id}` | _require_scope, _get_current_user | Detach a saved card. Disables auto-top-up if it was the default method. | internal | card and subscription management. The plan puts adding a card out of the agent surface entirely: card data belongs on a first-party page and the agent only ever references what is already on file |
| POST | `/api/billing/paypal/capture-order` | _require_scope | Capture a PayPal order and credit the wallet. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| POST | `/api/billing/paypal/create-order` | _require_scope | Create a PayPal order for depositing compute credits. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| GET | `/api/billing/paypal/enabled` | none found — verify by hand | Check whether PayPal is configured. | gap | P1 names crypto, Lightning and PayPal deposits as needing no browser and already scoped; no tool reaches any of them, so funding is tool-driven only on the card rail |
| POST | `/api/billing/paypal/marketplace/capture-order` | _require_scope | Capture a marketplace PayPal order and record the provider/platform split. | gap | P6's provider payout rail rather than a tenant funding one; no tool reaches it |
| POST | `/api/billing/paypal/marketplace/create-order` | _require_scope | Create a provider order using the exact PostgreSQL-settled job amount. | gap | P6's provider payout rail rather than a tenant funding one; no tool reaches it |
| POST | `/api/billing/paypal/webhook` | none found — verify by hand | PayPal webhook receiver — backup capture/credit when client capture fails. | internal | processor webhook; machine-to-machine and signature-verified, the caller is PayPal and never an agent |
| POST | `/api/billing/portal-session` | _require_scope, _get_current_user | Create a Stripe Customer Portal session for the authenticated wallet owner. | internal | card and subscription management. The plan puts adding a card out of the agent surface entirely: card data belongs on a first-party page and the agent only ever references what is already on file |
| POST | `/api/billing/refund` | _require_auth | Process a refund for a failed job. | internal | operator, testing and compliance surface — refunds, the billing sweep, test-state reset, the attestation document; not a tenant capability |
| POST | `/api/billing/setup-intent` | _require_scope, _get_current_user | Create a Stripe SetupIntent so the client can save a card off-session. | internal | card and subscription management. The plan puts adding a card out of the agent surface entirely: card data belongs on a first-party page and the agent only ever references what is already on file |
| GET | `/api/billing/usage/{customer_id}` | none found — verify by hand | Get usage summary for a customer. | gap | Usage summary for a billing period. get_spend_envelope answers burn rate and runway from the live wallet and running instances; it does not answer what a closed period consumed. |
| GET | `/api/billing/wallet/{customer_id}` | none found — verify by hand | Get credit wallet balance and status. | covered | MCP server calls this path |
| GET | `/api/billing/wallet/{customer_id}/depletion` | none found — verify by hand | Get real-time balance depletion projection. | covered | get_spend_envelope calls this path |
| POST | `/api/billing/wallet/{customer_id}/deposit` | none found — verify by hand | Deposit credits into a customer wallet. | internal | card and subscription management. The plan puts adding a card out of the agent surface entirely: card data belongs on a first-party page and the agent only ever references what is already on file |
| GET | `/api/billing/wallet/{customer_id}/history` | none found — verify by hand | Get transaction history for a wallet. | gap | get_wallet_balance returns the balance only. 'Where did my balance go' has no answer on the surface, which is the first question after an unexpected number. |
| POST | `/api/billing/wallet/{customer_id}/reset-testing` | _require_admin | Reset wallet balance and promo state for admin testing. | internal | operator, testing and compliance surface — refunds, the billing sweep, test-state reset, the attestation document; not a tenant capability |
| POST | `/api/pricing/estimate` | none found — verify by hand | Estimate what a job will cost, in CAD, before launching it. | covered | MCP server calls this path |
| GET | `/api/pricing/models` | none found — verify by hand | List all available GPU models with on-demand standard pricing. | redundant | A narrower projection of the table get_pricing_reference already returns: the same gpu_pricing rows filtered to tier='standard' and pricing_mode='on_demand'. |
| GET | `/api/pricing/rates` | none found — verify by hand | Compute effective GPU rate with all pricing variables. | gap | Returns the *breakdown* of an hourly rate — base, priority multiplier, premium, multi-GPU discount, tax. estimate_job_cost returns a total from /api/pricing/estimate; nothing lets an agent explain why a price is what it is. |
| GET | `/api/pricing/reference` | none found — verify by hand | Get reference GPU pricing table in CAD from live ``gpu_pricing``. | covered | MCP server calls this path |
| GET | `/api/pricing/reservations` | _require_scope, _require_auth | List the authenticated customer's reserved commitments + savings. | gap | Lists the caller's own commitments — the read half of POST /api/pricing/reserve, and unreachable for the same reason. |
| POST | `/api/pricing/reserve` | none found — verify by hand | Create a reserved pricing commitment for a customer. | gap | Creates a reserved pricing commitment. Money-moving with a term and no tool. Note for whoever builds it: POST /api/v2/marketplace/reservations also creates a reservation — which of the two is canonical is a product decision, and a tool must not be written against both. |
| GET | `/api/pricing/reserved-plans` | none found — verify by hand | List available reserved pricing tiers with discount percentages. | gap | The reserved-tier catalogue and its discounts. Nothing on the surface can tell a user a commitment would be cheaper than what they are doing. |
| GET | `/api/pricing/spot-quote` | none found — verify by hand | Live spot quote with tax breakdown for launch UI. | gap | A live spot quote with tax, for a launch decision. get_spot_prices returns pre-tax market rates, so the number an agent quotes is not the number charged. |
| GET | `/api/v2/billing/auto-topup` | _require_scope, _get_current_user | Get current auto-top-up configuration. | covered | configure_auto_topup calls this path |
| POST | `/api/v2/billing/auto-topup` | _require_scope, _get_current_user | Configure wallet auto-top-up via Stripe. | covered | configure_auto_topup calls this path |
| GET | `/api/v2/billing/pending-verification` | _get_current_user | Charges stopped waiting for the cardholder to satisfy their bank. | gap | an agent should be able to say 'one of your top-ups needs you'; no tool reaches it yet. Carries no client_secret by design |
| POST | `/api/v2/billing/pending-verification/{stripe_intent_id}/resume` | _get_current_user | Hand back the credential that lets the cardholder finish a challenge. | internal | returns a client_secret for the browser to finish an SCA challenge; a tool must never call it — Gate P1 forbids a secret in any tool result |
| POST | `/api/v2/billing/top-up` | _require_scope, _get_current_user | Charge a saved card now and credit the wallet when Stripe confirms. | covered | P1's top_up_wallet calls it; billing:write, idempotency-keyed |
| GET | `/billing` | _require_admin | Get all billing records and total revenue (platform admin only). | internal | operator, testing and compliance surface — refunds, the billing sweep, test-state reset, the attestation document; not a tenant capability |
| POST | `/billing/bill-all` | _require_admin | Bill all unbilled completed jobs. | internal | operator, testing and compliance surface — refunds, the billing sweep, test-state reset, the attestation document; not a tenant capability |
| POST | `/billing/bill/{job_id}` | _require_scope, _require_auth | Bill a specific completed job. | internal | operator, testing and compliance surface — refunds, the billing sweep, test-state reset, the attestation document; not a tenant capability |

## `routes/chat.py` — 12 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/ai/analytics` | _get_current_user | Stream AI-powered analytics insight/explanation via SSE. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| POST | `/api/ai/chat` | _get_current_user | Stream an AI assistant response with tool-calling support via SSE. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| POST | `/api/ai/confirm` | _get_current_user | Approve or reject a pending AI tool action. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| GET | `/api/ai/conversations` | _get_current_user | List the current user's AI assistant conversations. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| DELETE | `/api/ai/conversations/{conversation_id}` | _get_current_user | Delete an AI assistant conversation. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| GET | `/api/ai/conversations/{conversation_id}` | _get_current_user | Get messages for an AI assistant conversation. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| GET | `/api/ai/suggestions` | _get_current_user | Get context-aware suggestion chips for the AI assistant. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| POST | `/api/chat` | _require_scope, _get_current_user | Stream an AI chat response about Xcelsior via SSE. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| GET | `/api/chat/conversations` | _require_scope, _get_current_user | List recent conversations for the authenticated user. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| POST | `/api/chat/feedback` | _require_scope, _get_current_user | Record thumbs-up / thumbs-down feedback on a chat message. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| GET | `/api/chat/history/{conversation_id}` | _require_scope, _get_current_user | Return message history for an existing conversation. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |
| GET | `/api/chat/suggestions` | none found — verify by hand | Return suggested starter questions for the chat widget. | internal | the platform's own assistant and support chat; exposing it as a tool would nest one agent inside another rather than add a capability |

## `routes/cloudburst.py` — 1 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/v2/burst/status` | _get_current_user | Get cloud burst auto-scaling status. | internal | burst provisioning, driven by the scheduler |

## `routes/compliance.py` — 4 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/billing/gst-threshold` | _require_admin | Check platform-wide GST/HST small-supplier threshold status. | internal | Canadian tax and GST compliance surface — thresholds, rates and filing status. No phase of the plan touches it |
| GET | `/api/billing/gst-threshold/{provider_id}` | _require_provider_access | Check whether a specific provider has exceeded the $30,000 GST/HST | internal | Canadian tax and GST compliance surface — thresholds, rates and filing status. No phase of the plan touches it |
| GET | `/api/compliance/status` | none found — verify by hand | Return high-level compliance check summary with live verification. | internal | Canadian tax and GST compliance surface — thresholds, rates and filing status. No phase of the plan touches it |
| GET | `/api/compliance/tax-rates` | none found — verify by hand | Canadian GST/HST/PST rates by province for billing. | internal | Canadian tax and GST compliance surface — thresholds, rates and filing status. No phase of the plan touches it |

## `routes/control_plane_v1.py` — 24 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/v1/control-plane/commands/{command_id}/retry` | _require_host_operator | Redeliver only failed/dead-letter commands without changing identity. | covered | MCP server calls this path |
| GET | `/api/v1/control-plane/health` | none found — verify by hand | §18/§20.2 — control-plane health aggregate: outbox, findings, tasks. | covered | MCP server calls this path |
| GET | `/api/v1/control-plane/queue` | none found — verify by hand | §18/§20.2 — the queued instances awaiting placement, with reasons. Operator read. | internal | the control-plane command queue; operator diagnostics |
| GET | `/api/v1/control-plane/reconciliation-findings` | _require_scope, _require_auth | §18/§20.2 — reconciler findings feed (operator surface). | covered | MCP server calls this path |
| GET | `/api/v1/hosts/{host_id}/capacity` | none found — verify by hand | §18/§20.4 — a host's GPU capacity snapshot. Operator read. | covered | MCP server calls this path |
| POST | `/api/v1/hosts/{host_id}/drain` | _require_host_operator | §3.3 drain — stop **new** placements only. Running workloads keep running. | covered | MCP server calls this path |
| POST | `/api/v1/hosts/{host_id}/eviction-plans` | _require_host_operator | Create a human-approved plan; this never evicts workloads. | covered | MCP server calls this path |
| POST | `/api/v1/hosts/{host_id}/eviction-plans/{plan_id}/execute` | _require_host_operator | — | covered | evict_host_workloads calls this path, through the approval flow |
| POST | `/api/v1/hosts/{host_id}/evictions` | _require_host_operator | §3.3 evict — remove running workloads from a host, distinct from drain. | internal | direct eviction without a plan. The tool deliberately goes through eviction-plans instead, so this is the operator's unapproved path |
| GET | `/api/v1/hosts/{host_id}/observations` | none found — verify by hand | §18/§20.4 — recent worker-reported observations for a host. Operator read. | internal | control-plane telemetry for a host; operator diagnostics |
| POST | `/api/v1/hosts/{host_id}/undrain` | _require_host_operator | §3.3 undrain — return a drained host to service. | covered | MCP server calls this path |
| GET | `/api/v1/instances/{job_id}` | none found — verify by hand | Tenant-safe instance detail; cross-tenant identifiers are not-found. | covered | MCP server calls this path |
| GET | `/api/v1/instances/{job_id}/active-lease` | none found — verify by hand | Current lease health with a tenant-safe host alias and no credentials. | covered | MCP server calls this path |
| GET | `/api/v1/instances/{job_id}/attempts` | none found — verify by hand | §18 — the raw attempt records for a job. Tenant-scoped. | internal | per-attempt control-plane internals. get_instance_timeline covers what a tenant needs to see about an instance's history |
| GET | `/api/v1/instances/{job_id}/control-plane` | none found — verify by hand | §18/§20.3 — a job's control-plane state: phase, desired state, current | internal | control-plane state for one instance; operator diagnostics |
| GET | `/api/v1/instances/{job_id}/events` | none found — verify by hand | Durable, opaque-cursor event page for resumable MCP watches. | covered | MCP server calls this path |
| GET | `/api/v1/instances/{job_id}/placement-explanation` | none found — verify by hand | §3.2/§18 — the persisted placement explanation for the current attempt. | covered | MCP server calls this path |
| POST | `/api/v1/instances/{job_id}/reconcile` | _require_auth | §3.3/§18 reconcile — **enqueue** a reconcile for this instance. | covered | MCP server calls this path |
| POST | `/api/v1/instances/{job_id}/retry` | none found — verify by hand | §18 — re-enqueue a failed/stuck instance (does not run the queue inline). | covered | MCP server calls this path |
| GET | `/api/v1/instances/{job_id}/timeline` | none found — verify by hand | §20.3 — the attempt timeline for a job (reserve → command → lease → | covered | MCP server calls this path |
| GET | `/api/v1/mcp/activation-funnel` | _require_scope, _require_auth | Connector activation funnel, with per-surface attribution. | internal | product analytics on connector activation; not a tenant surface |
| GET | `/api/v1/mcp/tool-audit` | _require_auth | Customer-visible export of this tenant's own MCP tool-call audit trail. | covered | MCP server calls this path |
| POST | `/api/v1/mcp/tool-audit` | _require_auth | Persist a redacted MCP audit and its delivery intent atomically. | covered | MCP server calls this path |
| GET | `/api/v1/openapi.json` | none found — verify by hand | The versioned OpenAPI schema for the `/api/v1` surface (§18.1). | internal | the published API description itself, served for clients and generators to read |

## `routes/events.py` — 5 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/audit/instance/{job_id}` | _require_scope, _require_auth | Full auditable trail for a job — every event with hash chain. | gap | the audit trail for one instance. Gate P4 asks for 'one approval, three stages, one audit chain' asserted end to end; no tool reaches this |
| GET | `/api/audit/verify-chain` | _require_admin | Verify the tamper-evident hash chain on all events. | internal | verifies the audit log's hash chain end to end; a compliance and operator integrity check, not a tenant read |
| GET | `/api/events` | _require_admin | Get recent events across all entities (platform admin only). | gap | the account's activity stream. `events:read` is advertised in scopes_supported and no tool reads events, so a user grants it on first connection and nothing can use it |
| GET | `/api/events/leases/{job_id}` | _require_scope, _require_auth | Get active lease for a job. | gap | the account's activity stream. `events:read` is advertised in scopes_supported and no tool reads events, so a user grants it on first connection and nothing can use it |
| GET | `/api/events/{entity_type}/{entity_id}` | _require_scope, _require_auth | Get event history for a job or host. | gap | the account's activity stream. `events:read` is advertised in scopes_supported and no tool reads events, so a user grants it on first connection and nothing can use it |

## `routes/gpu.py` — 1 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/v2/gpu/available` | _require_scope, _get_current_user | List available GPU types with regions, VRAM, pricing, and counts. | covered | MCP server calls this path |

## `routes/health.py` — 34 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/` | none found — verify by hand | — | internal | server-rendered pages and a crawler text file, not an API surface |
| POST | `/_internal/legacy-auth/device` | none found — verify by hand | Initiate OAuth2 device authorization flow (RFC 8628). | internal | the superseded auth path, kept for older clients. The `_internal` prefix is the statement of intent |
| POST | `/_internal/legacy-auth/token` | none found — verify by hand | Poll for device authorization result (RFC 8628 §3.4). | internal | the superseded auth path, kept for older clients. The `_internal` prefix is the statement of intent |
| GET | `/_internal/legacy-auth/verify` | none found — verify by hand | Browser-facing page where users enter their device code. | internal | the superseded auth path, kept for older clients. The `_internal` prefix is the statement of intent |
| POST | `/_internal/legacy-auth/verify` | _get_current_user | Verify a device code by entering the user_code shown in the CLI. | internal | the superseded auth path, kept for older clients. The `_internal` prefix is the statement of intent |
| GET | `/alerts/config` | _require_admin | Get current alert config (passwords redacted). | internal | operator configuration — alert thresholds, the NFS mount, API token minting. Admin console, not the tool surface. The two alert paths are aliases of one handler |
| PUT | `/alerts/config` | _require_admin | Update alert config at runtime. | internal | operator configuration — alert thresholds, the NFS mount, API token minting. Admin console, not the tool surface. The two alert paths are aliases of one handler |
| GET | `/api/alerts/config` | _require_admin | Alias for /alerts/config with /api/ prefix. | internal | operator configuration — alert thresholds, the NFS mount, API token minting. Admin console, not the tool surface. The two alert paths are aliases of one handler |
| PUT | `/api/alerts/config` | _require_admin | Alias for PUT /alerts/config with /api/ prefix. | internal | operator configuration — alert thresholds, the NFS mount, API token minting. Admin console, not the tool surface. The two alert paths are aliases of one handler |
| GET | `/api/nfs/config` | _require_admin | Get current NFS configuration from environment (admin only). | internal | operator configuration — alert thresholds, the NFS mount, API token minting. Admin console, not the tool surface. The two alert paths are aliases of one handler |
| GET | `/api/slurm/instances` | _require_admin | List all tracked Slurm jobs. | internal | the Slurm bridge for HPC clusters — a separate scheduler integration, admin-only, and not the GPU-instance journey any P0-P7 phase describes |
| GET | `/api/slurm/profiles` | _require_admin | List available Slurm cluster profiles (Nibi, Graham, Narval, generic). | internal | the Slurm bridge for HPC clusters — a separate scheduler integration, admin-only, and not the GPU-instance journey any P0-P7 phase describes |
| GET | `/api/slurm/status/{slurm_job_id}` | _require_admin | Check the status of a Slurm job. | internal | the Slurm bridge for HPC clusters — a separate scheduler integration, admin-only, and not the GPU-instance journey any P0-P7 phase describes |
| POST | `/api/slurm/submit` | _require_admin | Submit an Xcelsior job to a Slurm cluster (HPC bridge). | internal | the Slurm bridge for HPC clusters — a separate scheduler integration, admin-only, and not the GPU-instance journey any P0-P7 phase describes |
| DELETE | `/api/slurm/{slurm_job_id}` | _require_admin | Cancel a Slurm job. | internal | the Slurm bridge for HPC clusters — a separate scheduler integration, admin-only, and not the GPU-instance journey any P0-P7 phase describes |
| GET | `/api/ssh/pubkey` | none found — verify by hand | Get the public key to add to hosts' authorized_keys. | internal | the platform's own host keypair. The public half is deliberately open because its purpose is to be pasted into authorized_keys; generating it is admin-only (see ae292c6). Never a tenant capability |
| GET | `/api/status` | none found — verify by hand | Aggregated per-service health for the CLI wizard preflight gate (and dashboard). | internal | liveness, readiness and the Prometheus scrape target; consumed by orchestrators and monitoring, deliberately unauthenticated |
| GET | `/api/stream` | none found — verify by hand | Server-Sent Events stream for real-time dashboard updates. | gap | SSE stream of live platform events with Last-Event-ID replay. The plan keeps watch_instance as the waiting primitive and this is the transport a richer one would use; no tool consumes it |
| POST | `/build` | _require_admin | Build a Docker image for a model. Optionally quantize and push (admin only). | internal | admin image building; P7 covers snapshots of a configured instance, which is a different mechanism from building an image from a model name |
| POST | `/build/{model}/dockerfile` | _require_admin | Preview the generated Dockerfile without building (admin only). | internal | admin image building; P7 covers snapshots of a configured instance, which is a different mechanism from building an image from a model name |
| GET | `/builds` | _require_admin | List all local build directories (admin only). | internal | admin image building; P7 covers snapshots of a configured instance, which is a different mechanism from building an image from a model name |
| GET | `/dashboard` | none found — verify by hand | The dashboard. HTML + JS. No React. No npm. No build step. | internal | server-rendered pages and a crawler text file, not an API surface |
| GET | `/healthz` | none found — verify by hand | Lightweight liveness probe for frontend polling. | internal | liveness, readiness and the Prometheus scrape target; consumed by orchestrators and monitoring, deliberately unauthenticated |
| GET | `/legacy` | none found — verify by hand | Legacy dashboard preserved at /legacy while Next.js serves /. | internal | server-rendered pages and a crawler text file, not an API surface |
| GET | `/legacy/{path:path}` | none found — verify by hand | Legacy dashboard preserved at /legacy while Next.js serves /. | internal | server-rendered pages and a crawler text file, not an API surface |
| GET | `/livez` | none found — verify by hand | Liveness (blueprint §21.3): the process is alive. No dependency calls. | internal | liveness, readiness and the Prometheus scrape target; consumed by orchestrators and monitoring, deliberately unauthenticated |
| GET | `/llms.txt` | none found — verify by hand | Serve LLM-optimized documentation for AI agents. | internal | server-rendered pages and a crawler text file, not an API surface |
| GET | `/metrics` | none found — verify by hand | — | covered | MCP server calls this path |
| GET | `/metrics/prometheus` | none found — verify by hand | Prometheus-compatible /metrics endpoint. | internal | liveness, readiness and the Prometheus scrape target; consumed by orchestrators and monitoring, deliberately unauthenticated |
| GET | `/readyz` | none found — verify by hand | — | covered | MCP server calls this path |
| POST | `/ssh/keygen` | _require_admin | Generate the platform's Ed25519 keypair for reaching provider hosts. | internal | the platform's own host keypair. The public half is deliberately open because its purpose is to be pasted into authorized_keys; generating it is admin-only (see ae292c6). Never a tenant capability |
| GET | `/ssh/pubkey` | none found — verify by hand | Get the public key to add to hosts' authorized_keys. | internal | the platform's own host keypair. The public half is deliberately open because its purpose is to be pasted into authorized_keys; generating it is admin-only (see ae292c6). Never a tenant capability |
| GET | `/startupz` | none found — verify by hand | Startup gate (blueprint §21.3 / §30): configuration and key material. | covered | MCP server calls this path |
| POST | `/token/generate` | _require_admin | Generate a secure random API token (admin only). | internal | operator configuration — alert thresholds, the NFS mount, API token minting. Admin console, not the tool surface. The two alert paths are aliases of one handler |

## `routes/host_admission.py` — 7 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/admin/admission-queue` | _require_admin | Hosts awaiting an operator decision, with evidence readiness. | internal | host admission handshake between platform and worker |
| POST | `/api/admin/hosts/{host_id}/admission-decisions` | _require_admin | Admit, reject, or revoke a host. Admin only. | internal | host admission handshake between platform and worker |
| POST | `/api/admin/hosts/{host_id}/authoritative-evidence` | _require_admin | Record operator-signed evidence. Admin only — this is the trust anchor. | internal | host admission handshake between platform and worker |
| POST | `/api/hosts/compatibility-sessions/{session_id}/evidence` | _require_scope, _require_auth | Submit signed helper output against an open session. | internal | host admission handshake between platform and worker |
| GET | `/api/hosts/{host_id}/admission` | _require_scope, _require_auth, _require_host_operator | Current admission state and what evidence is still outstanding. | internal | host admission handshake between platform and worker |
| POST | `/api/hosts/{host_id}/compatibility-sessions` | _require_scope, _require_auth, _require_host_operator | Open an expiring compatibility session for a host you operate. | internal | host admission handshake between platform and worker |
| POST | `/api/hosts/{host_id}/provider-evidence` | _require_scope, _require_auth, _require_host_operator | Record advisory evidence reported by the provider's own agent. | internal | host admission handshake between platform and worker |

## `routes/hosts.py` — 13 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/hosts/register` | _require_scope, _get_current_user | Register a host from the web dashboard UI. | gap | P6's supply side opens with registering a host; no tool reaches it |
| GET | `/api/hosts/{host_id}/spot-preview` | _require_scope, _require_auth | Live spot rate preview for a host GPU and optional provider floor. | gap | the preview a provider reads before setting a spot floor; P6, no tool |
| PATCH | `/api/hosts/{host_id}/spot-settings` | _require_scope, _require_auth, _require_host_operator | Update provider spot controls for a host (scheduler + marketplace offer). | gap | P6 names setting a spot floor as a provider journey; no tool reaches it |
| GET | `/compute-score/{host_id}` | none found — verify by hand | Get the compute score (XCU) for a host. | internal | platform scoring input for placement, not a surface a tenant or provider drives |
| GET | `/compute-scores` | none found — verify by hand | List compute scores for all hosts. | internal | platform scoring input for placement, not a surface a tenant or provider drives |
| PUT | `/host` | _require_scope, _require_auth, _require_host_operator | Register or update a host without trusting self-reported admission. | gap | Provider axis. Registering or updating a rig is how supply reaches the marketplace, and docs/mcp-provider-axis-plan.md records that the provider class has zero tools today. |
| DELETE | `/host/{host_id}` | _require_scope, _require_auth, _require_host_operator | Remove a host. | gap | Provider axis. Withdrawing a rig has no tool, so a provider agent can add capacity it cannot remove. |
| GET | `/host/{host_id}` | _require_scope, _require_auth | Get a single host by ID. | gap | Provider axis. get_host_capacity is an operator tool about scheduling headroom, not the host record a provider owns. |
| POST | `/host/{host_id}/drain` | _require_admin | Stop new placements on a host without evicting active instances. | covered | MCP server calls this path |
| GET | `/host/{host_id}/maintenance` | _require_admin | Return maintenance readiness for a host. | gap | Operator profile. drain_host and undrain_host act; nothing reads whether a host is ready to be drained, which is the question asked before acting. |
| POST | `/host/{host_id}/undrain` | _require_admin | Restore a drained host to active or pending status. | redundant | superseded by /api/v1/hosts/{host_id}/undrain, which is what the undrain_host tool calls |
| GET | `/hosts` | _require_scope, _require_auth | List hosts the caller may see. | gap | Provider axis. Nothing enumerates the caller's own hosts. |
| POST | `/hosts/check` | _require_admin | Ping all hosts and update status. | internal | operator health sweep across hosts; no journey calls it |

## `routes/inference.py` — 7 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/inference` | _require_scope, _require_auth | Submit a serverless inference request. | redundant | The pre-v1 submit route for the same direct-inference capability as POST /v1/inference/async. Both mint a job; the v1 pair is the surface the API advertises as OpenAI-compatible, so that is the one a tool should target. |
| GET | `/api/inference/models/available` | _require_scope, _require_auth | List available inference models and their resource requirements. | gap | Lists inference models and their resource needs. list_serverless_endpoints shows what is already deployed, not what could be; choosing a model has no tool. |
| GET | `/api/inference/{job_id}` | _require_scope, _require_auth | Get inference results for a submitted request. | redundant | Pre-v1 result read, superseded by GET /v1/inference/{job_id}. |
| POST | `/api/inference/{job_id}/result` | _require_worker_status_update | Worker callback: post inference results. Internal use. | internal | worker callback posting a finished inference result; authenticated as the fleet, never an agent |
| POST | `/v1/inference` | _require_auth | Synchronous inference endpoint (OpenAI-compatible path). | gap | Synchronous OpenAI-compatible inference, distinct from the endpoint-based serverless path run_serverless_job covers. An agent cannot use the compatible surface the API advertises. |
| POST | `/v1/inference/async` | _require_auth | Asynchronous inference — returns job_id immediately for polling. | gap | The async half of the same direct-inference surface, returning a job_id. |
| GET | `/v1/inference/{job_id}` | _require_scope, _require_auth | Poll for inference results by job_id. | gap | Polls the job the async route returns; unreachable for the same reason. |

## `routes/instances.py` — 35 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/admin/instances/{job_id}/reinject-shell` | _require_auth | Admin-only: re-apply MOTD/PS1/sshd setup to the instance's container. | internal | admin-only re-injection of shell setup. The guard is inline (`role == admin`) rather than `_require_admin`, so the inventory's recovered auth column understates it |
| GET | `/api/images/templates` | none found — verify by hand | Return the authoritative list of validated container image templates. | gap | P7's image library with provenance — commit an instance to a reusable image and manage the result; no tool reaches it |
| POST | `/api/instances/{job_id}/stream-ticket` | _require_scope, _require_auth | Issue a short-lived one-time WebSocket ticket for instance streaming. | gap | P2's launch-to-connected journey needs these; scoped for instances:connect in 406c0a1 but no tool reaches them yet |
| GET | `/api/instances/{job_id}/telemetry` | _require_scope, _require_auth | Latest GPU telemetry for the host running this instance (for agents/MCP watch_instance). | covered | MCP server calls this path |
| POST | `/api/v2/scheduler/process-binpack` | _require_admin | Process job queue using best-fit-decreasing bin packing (admin only). | internal | operator action on the scheduler or a stuck instance; admin-guarded and outside every tenant journey |
| POST | `/failover` | _require_admin | Run a full failover cycle (admin only). | internal | operator action on the scheduler or a stuck instance; admin-guarded and outside every tenant journey |
| POST | `/instance` | _require_scope, _require_auth | Submit a job to the queue or directly assign to a host. | covered | MCP server calls this path |
| GET | `/instance/{job_id}` | _require_auth | Get a specific instance by ID, enriched with connection info. | covered | MCP server calls this path |
| PATCH | `/instance/{job_id}` | _require_worker_status_update | Update a job's status. | covered | MCP server calls this path |
| PATCH | `/instance/{job_id}/name` | _require_scope, _require_auth | Rename an instance (owner or admin only). | gap | Renaming is cosmetic but it is how a user keeps a fleet legible, and an agent that launches instances cannot label them. |
| POST | `/instance/{job_id}/requeue` | _require_scope, _require_auth | Manually requeue a failed, cancelled, or stuck job. | gap | retry_instance calls /api/v1/instances/{job_id}/retry, not this. Whether the two are one authority or two is worth settling before a tool points here. |
| GET | `/instances` | _require_scope, _require_auth | List instances for the authenticated user (admins see all). | covered | MCP server calls this path |
| GET | `/instances/{job_id}/auto-launch` | _require_scope, _require_auth | Return the public URL + service credentials for each auto-launch service. | gap | P2's launch-to-connected journey needs these; scoped for instances:connect in 406c0a1 but no tool reaches them yet |
| POST | `/instances/{job_id}/auto-launch/report` | _require_agent_auth | Internal: worker agent reports that auto-launch finished. | internal | worker-agent callback reporting instance state back to the platform; authenticated as the fleet, never an agent |
| POST | `/instances/{job_id}/cancel` | _require_scope, _require_auth | Cancel a running or queued instance. For interactive instances, stops the container. | covered | MCP server calls this path |
| POST | `/instances/{job_id}/expose` | _require_scope, _require_auth | Return the public HTTPS URL for a declared container port. | gap | P2's launch-to-connected journey needs these; scoped for instances:connect in 406c0a1 but no tool reaches them yet |
| POST | `/instances/{job_id}/http-ports/report` | _require_agent_auth | Internal: worker agent reports the final port-publish map. | internal | worker-agent callback reporting instance state back to the platform; authenticated as the fleet, never an agent |
| POST | `/instances/{job_id}/lock` | none found — verify by hand | Lock the instance against accidental modification. | gap | Marks an instance protected from accidental modification. Precisely the guard a user would want *because* an agent can now cancel and terminate. |
| GET | `/instances/{job_id}/logs` | _require_scope, _require_auth | Get buffered log lines for a job (non-streaming). | covered | MCP server calls this path |
| GET | `/instances/{job_id}/logs/download` | _require_auth | Download all available logs for a job as a plain text file. | redundant | the same logs get_instance_logs already reaches, delivered as a plain-text file for a browser. Same capability, different delivery — the shape ruled redundant for the OpenAI shims in 1cabc0e |
| GET | `/instances/{job_id}/logs/stream` | _require_scope, _require_auth | Stream real-time logs for a specific job via Server-Sent Events. | gap | a live tail rather than a snapshot, which is a different capability from the one get_instance_logs provides. Same reasoning as the serverless stream route |
| POST | `/instances/{job_id}/reset` | none found — verify by hand | Reset the container (running instance only). | gap | Restarts the container in place — the middle option between 'do nothing' and cancel_instance, which ends the run and loses uncheckpointed work. |
| POST | `/instances/{job_id}/restart` | none found — verify by hand | Restart an instance. Container data is fully preserved. | gap | stop/start/restart preserve the container, which is the lifecycle the platform chose over pause/resume; no tool reaches them, so an agent can only cancel or terminate |
| POST | `/instances/{job_id}/snapshot` | _require_scope, _require_auth | Create a user image from a running container (`docker commit`). | gap | P7 commits a configured instance to a reusable image; this is that step and no tool reaches it |
| POST | `/instances/{job_id}/start` | none found — verify by hand | Start a stopped instance. | gap | stop/start/restart preserve the container, which is the lifecycle the platform chose over pause/resume; no tool reaches them, so an agent can only cancel or terminate |
| POST | `/instances/{job_id}/stop` | none found — verify by hand | Gracefully stop a running instance. | gap | stop/start/restart preserve the container, which is the lifecycle the platform chose over pause/resume; no tool reaches them, so an agent can only cancel or terminate |
| POST | `/instances/{job_id}/terminate` | none found — verify by hand | Permanently terminate an instance. This action is irreversible. | covered | MCP server calls this path |
| POST | `/instances/{job_id}/unlock` | none found — verify by hand | Remove the lock flag. | gap | The release half of the lock above. |
| GET | `/internal/route/{slug}/{port}` | none found — verify by hand | nginx auth_request target: resolve <slug>-<port>.xcelsior.ca. | internal | internal reverse-proxy route resolution for exposed ports |
| POST | `/queue/process` | _require_admin | Process the job queue — assign jobs to hosts (admin only). | internal | operator action on the scheduler or a stuck instance; admin-guarded and outside every tenant journey |
| GET | `/tiers` | none found — verify by hand | List all priority tiers with their multipliers. | covered | MCP server calls this path |
| GET | `/user-images` | _require_auth | List saved pod templates. | gap | P7's image library with provenance — commit an instance to a reusable image and manage the result; no tool reaches it |
| DELETE | `/user-images/{image_id}` | _require_auth | Soft-delete a user image record. (Underlying docker image not removed.) | gap | P7's image library with provenance — commit an instance to a reusable image and manage the result; no tool reaches it |
| PATCH | `/user-images/{image_id}` | _require_auth | Phase D — update description/labels/visibility/star on a template. | gap | P7's image library with provenance — commit an instance to a reusable image and manage the result; no tool reaches it |
| POST | `/user-images/{image_id}/complete` | _require_agent_auth | Internal: worker agent callback after `docker commit` finishes. | internal | worker-agent callback reporting instance state back to the platform; authenticated as the fleet, never an agent |

## `routes/marketplace.py` — 15 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/v2/marketplace/allocate` | _require_scope, _get_current_user | Allocate GPUs from an offer for a job. Atomic — prevents double-sell. | internal | The marketplace engine's atomic claim on an offer, behind marketplace:write — a scope no tool holds and Quick Connect does not grant. The customer path is the launch plan; exposing direct allocation would put GPUs on a job without the approval gate create_instance is built around. |
| POST | `/api/v2/marketplace/offers` | _require_scope, _get_current_user | Create or update a GPU offer on the marketplace. | gap | P6's publish-capacity step for a provider; no tool reaches it |
| POST | `/api/v2/marketplace/release/{allocation_id}` | _require_scope, _get_current_user | Release a GPU allocation (job completed/failed). | internal | The release half of the allocation primitive above, same scope, same reason: it is the scheduler's bookkeeping, not a user action. |
| POST | `/api/v2/marketplace/reservations` | _require_scope, _get_current_user | Create a reserved instance commitment. | gap | Creates a reserved-capacity commitment — a spending decision with a term. No tool makes one, so the cheaper-than-on-demand path is browser-only. |
| DELETE | `/api/v2/marketplace/reservations/{reservation_id}` | _require_scope, _get_current_user | Cancel a reserved instance commitment early. | gap | Cancelling a commitment early has no tool, the same asymmetry as the DELETEs above: reachable to enter, unreachable to exit. |
| POST | `/api/v2/marketplace/search` | none found — verify by hand | Search available GPU offers with filters. | covered | MCP server calls this path |
| GET | `/api/v2/marketplace/spot-prices` | none found — verify by hand | Get current spot prices for all GPU models. | covered | MCP server calls this path |
| GET | `/api/v2/marketplace/spot-prices/{gpu_model}/history` | none found — verify by hand | Get spot price history for a GPU model. | gap | get_spot_prices returns the current rate only. History is what makes a spot-vs-on-demand recommendation defensible rather than a snapshot guess. |
| GET | `/api/v2/marketplace/stats` | none found — verify by hand | Get marketplace aggregate statistics. | gap | Aggregate supply and demand. Nothing on the surface answers 'is the market tight right now', which is the context for a price. |
| GET | `/marketplace` | _require_scope, _get_current_user | Browse marketplace listings. | redundant | Legacy unfiltered browse — get_marketplace(active_only=True), the same source the covered v2 search reads, with no filters applied. |
| POST | `/marketplace/bill/{job_id}` | _require_scope, _require_auth | Bill a marketplace job — split between host and platform. | internal | billing sweep over a completed job; the scheduler calls it |
| POST | `/marketplace/list` | _require_scope, _get_current_user | List an active, authoritatively admitted rig on the marketplace. | gap | Provider axis. Listing an admitted rig for sale is the provider's core write and is unreachable. |
| GET | `/marketplace/search` | none found — verify by hand | Search marketplace listings with filters and sorting. | redundant | Legacy filtered search, superseded by POST /api/v2/marketplace/search, which is what search_marketplace calls. Worth noting while classifying: this one takes no Request and performs no auth check at all — the listings are public, so it is not a leak, but it is the only marketplace read with no scope gate. |
| GET | `/marketplace/stats` | _require_admin | Marketplace aggregate stats (platform admin only). | gap | Operator profile. Platform-admin aggregates (_require_admin), distinct from the v2 row above; no operator tool reads them. |
| DELETE | `/marketplace/{host_id}` | _require_scope, _get_current_user | Remove a rig from the marketplace. | gap | Provider axis. Delisting a rig — the counterpart of POST /marketplace/list. |

## `routes/mfa.py` — 16 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| DELETE | `/api/auth/mfa/all` | _require_scope, _get_current_user | Disable all MFA methods for the user. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/backup-codes/regenerate` | _require_scope, _get_current_user | Regenerate backup recovery codes. Invalidates all previous codes. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| GET | `/api/auth/mfa/methods` | _require_scope, _get_current_user | List the user's configured MFA methods. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/passkey/authenticate-complete` | none found — verify by hand | Verify passkey authentication to complete MFA login. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/passkey/authenticate-options` | none found — verify by hand | Generate WebAuthn authentication options during login MFA challenge. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/passkey/delete` | _require_scope, _get_current_user | Remove a registered passkey. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/passkey/register-complete` | _require_scope, _get_current_user | Complete passkey registration with the browser's attestation response. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/passkey/register-options` | _require_scope, _get_current_user | Generate WebAuthn registration options for adding a new passkey. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| DELETE | `/api/auth/mfa/sms` | _require_scope, _get_current_user | Disable and remove SMS MFA. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/sms/send` | none found — verify by hand | Send an SMS verification code during MFA login challenge. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/sms/setup` | _require_scope, _get_current_user | Register a phone number for SMS MFA. Sends a verification code. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/sms/verify` | _require_scope, _get_current_user | Verify the SMS code to complete SMS MFA setup. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| DELETE | `/api/auth/mfa/totp` | _require_scope, _get_current_user | Disable and remove TOTP. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/totp/setup` | _require_scope, _get_current_user | Generate a TOTP secret and QR code URI for setup. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/totp/verify` | _require_scope, _get_current_user | Verify a TOTP code to complete setup and enable TOTP. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |
| POST | `/api/auth/mfa/verify` | none found — verify by hand | Verify MFA code during login to complete authentication. | internal | account security enrolment (TOTP, SMS, passkeys, backup codes); first-party dashboard only and never an agent capability |

## `routes/notifications.py` — 8 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/notifications` | _require_scope, _get_current_user | List notifications for the current user. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| DELETE | `/api/notifications/push/subscription` | _require_scope, _get_current_user | Revoke the current browser's push subscription. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| GET | `/api/notifications/push/subscription` | _require_scope, _get_current_user | Get push subscription capability for the current user. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| POST | `/api/notifications/push/subscription` | _require_scope, _get_current_user | Create or refresh the current browser's push subscription. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| POST | `/api/notifications/read-all` | _require_scope, _get_current_user | Mark all notifications as read for the current user. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| GET | `/api/notifications/unread-count` | _require_scope, _get_current_user | Get the unread notification count for the current user. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| DELETE | `/api/notifications/{notification_id}` | _require_scope, _get_current_user | Delete a single notification. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |
| POST | `/api/notifications/{notification_id}/read` | _require_scope, _get_current_user | Mark a single notification as read. | internal | dashboard notification centre and web-push subscriptions; no journey in the plan reads or clears a user's notifications |

## `routes/platform.py` — 7 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/v2/platform/attestation-schema` | none found — verify by hand | Merged host attestation schema (row 8/10). | internal | published platform document — compliance and partnership material, served for humans to read |
| GET | `/api/v2/platform/capacity-forecast/{endpoint_id}` | _require_auth | 14-day demand forecast + breach alert for an endpoint (row 11). | gap | forward capacity for an endpoint — the input an agent would need to decide whether to schedule now or wait; no tool reaches it |
| GET | `/api/v2/platform/h100-partner` | none found — verify by hand | H100 attested-host partner pipeline (row 10). | internal | published platform document — compliance and partnership material, served for humans to read |
| GET | `/api/v2/platform/ops-plan` | none found — verify by hand | 18-month platform ops plan (row 9). | internal | published platform document — compliance and partnership material, served for humans to read |
| GET | `/api/v2/platform/scip-alignment` | none found — verify by hand | SCIP alignment one-pager + application scan (row 9). | internal | published platform document — compliance and partnership material, served for humans to read |
| GET | `/api/v2/platform/scip-loi` | none found — verify by hand | Partner LOI submitted for SCIP / attested-host pipeline (row 8). | internal | published platform document — compliance and partnership material, served for humans to read |
| POST | `/api/v2/serverless/should-i-run-this` | _require_auth | PEL/admin guardrail: wallet + token/GPU estimate before serverless spend (row 12). | covered | MCP server calls this path |

## `routes/privacy.py` — 13 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/privacy/config` | _require_scope, _require_auth | Save privacy configuration for an organization. | internal | retention configuration and reporting — the policy, its current state, and the expiry sweep. Operator and compliance surface |
| GET | `/api/privacy/config/{org_id}` | _require_scope, _require_auth | Get privacy configuration for an organization (defaults to STRICT). | internal | retention configuration and reporting — the policy, its current state, and the expiry sweep. Operator and compliance surface |
| POST | `/api/privacy/consent` | _require_scope, _require_auth | Record explicit consent (privacy principle: Consent). | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| GET | `/api/privacy/consent/{entity_id}` | _require_scope, _require_auth | Get all consent records for an entity (privacy: Individual Access). | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| DELETE | `/api/privacy/consent/{entity_id}/{consent_type}` | _require_scope, _require_auth | Revoke consent (privacy: individuals can withdraw consent). | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| POST | `/api/privacy/purge-expired` | _require_scope, _require_auth | Purge all expired retention records (daily maintenance). | internal | retention configuration and reporting — the policy, its current state, and the expiry sweep. Operator and compliance surface |
| GET | `/api/privacy/retention-policies` | _require_scope, _get_current_user | Data retention policies per privacy fair information principles. | internal | retention configuration and reporting — the policy, its current state, and the expiry sweep. Operator and compliance surface |
| GET | `/api/privacy/retention-summary` | _require_scope, _require_auth | Current retention status across all data categories. | internal | retention configuration and reporting — the policy, its current state, and the expiry sweep. Operator and compliance surface |
| POST | `/api/v2/privacy/consent` | none found — verify by hand | Record CASL consent for a purpose. | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| DELETE | `/api/v2/privacy/consent/{purpose}` | none found — verify by hand | Withdraw CASL consent for a purpose (unsubscribe). | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| GET | `/api/v2/privacy/consents` | none found — verify by hand | List all consent records for the current user. | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| POST | `/api/v2/privacy/erase` | none found — verify by hand | Create an inspectable, asynchronous right-to-erasure request. | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |
| GET | `/api/v2/privacy/erase/{request_id}` | _get_current_user | Return honest per-store status for one erasure request. | internal | consent and erasure records under privacy law. Two generations of the API sit side by side; both are the account holder's own compliance surface and no phase asks an agent to consent or erase on someone's behalf |

## `routes/providers.py` — 15 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/providers` | _require_scope, _require_auth | List provider accounts visible to the caller (own account, or all for admins). | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/register` | _require_scope, _require_auth | Register a GPU provider with Stripe Connect onboarding. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/webhook` | none found — verify by hand | Handle Stripe Connect webhooks (account.updated, payment_intent.succeeded, etc.). | internal | processor webhook; machine-to-machine and signature-verified, and P1's only completion signal — the caller is Stripe, never an agent |
| GET | `/api/providers/{provider_id}` | _require_scope, _require_provider_access | Get provider account details including company info and payout status. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/abandon-onboarding` | _require_scope, _require_provider_access | Mark a provider's onboarding as abandoned. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/account-session` | _require_scope, _require_provider_access | Create a Stripe Connect AccountSession for embedded onboarding UI. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| GET | `/api/providers/{provider_id}/earnings` | _require_scope, _require_provider_access | Get aggregate earnings and payout history for a provider. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/incorporation` | _require_scope, _require_provider_access | Link an uploaded incorporation document to a provider account. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/payout` | _require_scope, _require_provider_access | Settle an eligible job from PostgreSQL billing authority. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| GET | `/api/providers/{provider_id}/paypal` | _require_scope, _require_provider_access | PayPal Complete Payments onboarding status for a provider. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/paypal/disconnect` | _require_scope, _require_provider_access | Unlink a provider's PayPal seller account. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/paypal/onboard` | _require_scope, _require_provider_access | Start or resume PayPal seller onboarding (partner referral link). | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/paypal/refresh` | _require_scope, _require_provider_access | Poll PayPal for seller merchant_id after onboarding completes. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/resume-onboarding` | _require_scope, _require_provider_access | Generate a fresh Stripe onboarding URL for a provider stuck in onboarding. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/providers/{provider_id}/stripe/disconnect` | _require_scope, _require_provider_access | Unlink a provider's Stripe account so they can re-run the connect flow. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |

## `routes/reputation.py` — 9 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/reputation/leaderboard` | _require_scope, _require_auth | Top hosts/users by reputation score. | gap | P5 reputation-aware placement needs these as tools |
| GET | `/api/reputation/me` | _require_scope, _require_auth | Get reputation for the currently authenticated user. | gap | P5 reputation-aware placement needs these as tools |
| POST | `/api/reputation/me/claim` | _require_scope, _require_auth | Grant every milestone the caller has genuinely earned but not yet claimed. | gap | P5 reputation-aware placement needs these as tools |
| GET | `/api/reputation/me/journey` | _require_scope, _require_auth | The caller's gamified milestone journey with live progress. | gap | P5 reputation-aware placement needs these as tools |
| POST | `/api/reputation/verify` | _require_admin | Grant a verification badge to a host/user (platform admin only). | gap | P5 reputation-aware placement needs these as tools |
| GET | `/api/reputation/{entity_id}` | _require_scope, _require_auth | Get reputation score and tier for a host or user. | gap | P5 reputation-aware placement needs these as tools |
| GET | `/api/reputation/{entity_id}/breakdown` | _require_scope, _require_auth | Get a detailed breakdown of how a reputation score is calculated. | gap | P5 reputation-aware placement needs these as tools |
| GET | `/api/reputation/{entity_id}/history` | _require_scope, _require_auth | Get reputation event history. | gap | P5 reputation-aware placement needs these as tools |
| GET | `/api/trust-tiers` | _require_scope, _require_auth | Return all six trust tiers with thresholds, perks, and unlock requirements. | gap | P5 reputation-aware placement needs these as tools |

## `routes/serverless.py` — 59 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/v1/serverless/endpoint-plans` | _require_scope, _require_auth | Persist an encrypted, server-bound approval plan for endpoint spend. | covered | MCP server calls this path |
| POST | `/api/v1/serverless/endpoint-plans/{plan_id}/execute` | _require_scope, _require_auth | Idempotently create one endpoint for one approved plan. | covered | MCP server calls this path |
| GET | `/api/v2/inference/endpoints` | none found — verify by hand | — | redundant | a compatibility shim: the handler is `api_inference_compat_*`, tagged 'Serverless (compat)', and its whole body delegates to the corresponding api_serverless_* function. Folded into the serverless surface the MCP already calls, and the code declares it rather than leaving it to be inferred |
| POST | `/api/v2/inference/endpoints` | none found — verify by hand | — | redundant | a compatibility shim: the handler is `api_inference_compat_*`, tagged 'Serverless (compat)', and its whole body delegates to the corresponding api_serverless_* function. Folded into the serverless surface the MCP already calls, and the code declares it rather than leaving it to be inferred |
| DELETE | `/api/v2/inference/endpoints/{endpoint_id}` | none found — verify by hand | — | redundant | a compatibility shim: the handler is `api_inference_compat_*`, tagged 'Serverless (compat)', and its whole body delegates to the corresponding api_serverless_* function. Folded into the serverless surface the MCP already calls, and the code declares it rather than leaving it to be inferred |
| GET | `/api/v2/inference/endpoints/{endpoint_id}` | none found — verify by hand | — | redundant | a compatibility shim: the handler is `api_inference_compat_*`, tagged 'Serverless (compat)', and its whole body delegates to the corresponding api_serverless_* function. Folded into the serverless surface the MCP already calls, and the code declares it rather than leaving it to be inferred |
| GET | `/api/v2/inference/endpoints/{endpoint_id}/health` | none found — verify by hand | — | redundant | a compatibility shim: the handler is `api_inference_compat_*`, tagged 'Serverless (compat)', and its whole body delegates to the corresponding api_serverless_* function. Folded into the serverless surface the MCP already calls, and the code declares it rather than leaving it to be inferred |
| GET | `/api/v2/inference/endpoints/{endpoint_id}/usage` | none found — verify by hand | — | redundant | a compatibility shim: the handler is `api_inference_compat_*`, tagged 'Serverless (compat)', and its whole body delegates to the corresponding api_serverless_* function. Folded into the serverless surface the MCP already calls, and the code declares it rather than leaving it to be inferred |
| GET | `/api/v2/serverless/batches/{batch_id}` | _require_auth | — | gap | No tool reads batch status, the necessary companion to submitting one. |
| GET | `/api/v2/serverless/enabled` | _get_current_user | Feature-flag probe for dashboard rollout (session optional). | internal | Feature-flag probe for the dashboard's own rollout, per its docstring. An agent learns serverless is available by the tools existing; a flag for staging a UI is not a capability. |
| GET | `/api/v2/serverless/endpoints` | _require_auth | — | covered | MCP server calls this path |
| POST | `/api/v2/serverless/endpoints` | _require_scope, _require_auth | — | covered | MCP server calls this path |
| DELETE | `/api/v2/serverless/endpoints/{endpoint_id}` | _require_auth | — | gap | No tool deletes an endpoint. This is the money-stopping action for serverless, the counterpart of cancel_instance, and it is unreachable. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}` | _require_auth | — | gap | No tool reads one endpoint. list_serverless_endpoints returns all, so an agent holding an endpoint_id must fetch every endpoint and filter — the asymmetry get_instance/list_instances does not have on the compute side. |
| PATCH | `/api/v2/serverless/endpoints/{endpoint_id}` | _require_auth | — | gap | No tool changes an endpoint's configuration. Scaling and model settings are exactly what an agent tuning cost or latency would need to reach. |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/batches` | none found — verify by hand | OpenAI-style async batch — discounted token/GPU billing for bulk inference. | gap | No tool submits a batch. Per its summary this is the discounted token/GPU billing path, so the cheaper way to run bulk inference is reachable only from a browser. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/health` | _require_auth | — | gap | No tool reads endpoint health. 'Is my endpoint actually serving?' has no answer on the tool surface. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/jobs` | _require_auth | — | gap | No tool lists an endpoint's jobs. get_serverless_job_status answers about a job_id already known; nothing enumerates them. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/jobs/{job_id}` | _require_auth | — | redundant | Byte-identical to GET /v1/serverless/{endpoint_id}/status/{job_id}: same keys from the same _repo().get_job(job_id, endpoint_id=...) call, differing only in auth helper. The v1 route is what get_serverless_job_status calls, so this is a second door to a covered capability. |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/jobs/{job_id}/cancel` | _require_auth | — | gap | No tool cancels a running inference job. Money-stopping and unreachable, the same shape as the DELETE above. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/jobs/{job_id}/stream` | _require_auth | — | gap | No tool follows a job. watch_instance is the waiting primitive for compute and has no serverless counterpart; a tool would poll rather than hold this SSE stream open. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/keys` | _require_auth | — | internal | per-endpoint API keys. Issuing a credential that calls the endpoint is the same escalation shape as minting an agent key: a tool that could do it could widen its own reach |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/keys` | _require_auth | — | internal | per-endpoint API keys. Issuing a credential that calls the endpoint is the same escalation shape as minting an agent key: a tool that could do it could widen its own reach |
| DELETE | `/api/v2/serverless/endpoints/{endpoint_id}/keys/{key_id}` | _require_auth | — | internal | per-endpoint API keys. Issuing a credential that calls the endpoint is the same escalation shape as minting an agent key: a tool that could do it could widen its own reach |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/metrics` | _require_auth | — | gap | No tool reads endpoint metrics. The canonical read; /usage is its dashboard alias. |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/test/openai/v1/chat/completions` | _require_auth | — | internal | the dashboard's try-it console for an endpoint; the tool surface invokes endpoints through /v1/serverless instead |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/test/run` | _require_auth | — | internal | the dashboard's try-it console for an endpoint; the tool surface invokes endpoints through /v1/serverless instead |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/test/runsync` | _require_auth | — | internal | the dashboard's try-it console for an endpoint; the tool surface invokes endpoints through /v1/serverless instead |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/usage` | _require_auth | Alias for metrics — dashboard compatibility. | redundant | Its own docstring says 'Alias for metrics — dashboard compatibility'. Not byte-identical — it enriches with pricing and SLO — but it exists to shape get_endpoint_metrics for the dashboard, so /metrics is the row to cover. |
| POST | `/api/v2/serverless/endpoints/{endpoint_id}/warm` | _require_auth | — | gap | No tool pre-warms. Cold start is the headline cost of serverless inference and the one thing an agent could do about it before a latency-sensitive run. |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/workers` | _require_auth | — | internal | worker introspection for one endpoint — roster, logs and telemetry. Operator and dashboard debugging |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/workers/{worker_id}/logs` | _require_auth | — | internal | worker introspection for one endpoint — roster, logs and telemetry. Operator and dashboard debugging |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/workers/{worker_id}/logs/stream` | _require_auth | — | internal | worker introspection for one endpoint — roster, logs and telemetry. Operator and dashboard debugging |
| GET | `/api/v2/serverless/endpoints/{endpoint_id}/workers/{worker_id}/telemetry` | _require_auth | — | internal | worker introspection for one endpoint — roster, logs and telemetry. Operator and dashboard debugging |
| POST | `/api/v2/serverless/github/resolve` | _require_auth | Resolve a GitHub repo URL to the default GHCR image reference. | internal | dashboard helper — resolving a GitHub source and reading preset token pricing while composing an endpoint |
| GET | `/api/v2/serverless/preset-token-pricing` | _require_auth | Per-million token rates for preset models (single source: metering.py). | internal | dashboard helper — resolving a GitHub source and reading preset token pricing while composing an endpoint |
| POST | `/api/v2/serverless/workers/{worker_id}/exited` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| POST | `/api/v2/serverless/workers/{worker_id}/heartbeat` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| POST | `/api/v2/serverless/workers/{worker_id}/jobs/claim` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| GET | `/api/v2/serverless/workers/{worker_id}/jobs/{job_id}` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| POST | `/api/v2/serverless/workers/{worker_id}/jobs/{job_id}/complete` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| POST | `/api/v2/serverless/workers/{worker_id}/jobs/{job_id}/events` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| POST | `/api/v2/serverless/workers/{worker_id}/ready` | _require_worker_callback | — | internal | serverless worker callback — claim, heartbeat, ready, complete, exit. Authenticated by _require_worker_callback as the fleet; an agent is never the caller |
| POST | `/v1/serverless/{endpoint_id}/cancel/{job_id}` | none found — verify by hand | — | gap | stops a running serverless job. cancel_instance covers GPU instances; nothing covers this. |
| POST | `/v1/serverless/{endpoint_id}/openai/v1/chat/completions` | none found — verify by hand | — | redundant | an OpenAI-protocol shim over the same capability run_serverless_job already reaches. It exists so an OpenAI SDK can be pointed at the endpoint, which is a client-compatibility surface rather than a tool one. |
| POST | `/v1/serverless/{endpoint_id}/openai/v1/embeddings` | none found — verify by hand | — | redundant | an OpenAI-protocol shim over the same capability run_serverless_job already reaches. It exists so an OpenAI SDK can be pointed at the endpoint, which is a client-compatibility surface rather than a tool one. |
| GET | `/v1/serverless/{endpoint_id}/openai/v1/models` | none found — verify by hand | — | redundant | an OpenAI-protocol shim over the same capability run_serverless_job already reaches. It exists so an OpenAI SDK can be pointed at the endpoint, which is a client-compatibility surface rather than a tool one. |
| POST | `/v1/serverless/{endpoint_id}/run` | none found — verify by hand | — | covered | MCP server calls this path |
| POST | `/v1/serverless/{endpoint_id}/runsync` | none found — verify by hand | — | gap | the synchronous variant of run: it blocks for the result instead of returning a job id. Different behaviour, not a second spelling, and no tool reaches it. |
| GET | `/v1/serverless/{endpoint_id}/status/{job_id}` | none found — verify by hand | — | covered | get_serverless_job_status calls this path |
| GET | `/v1/serverless/{endpoint_id}/stream/{job_id}` | none found — verify by hand | — | gap | streams a job's output as it is produced; no tool reaches it, so an agent can only poll for completion. |
| POST | `/v1/serverless/{endpoint_id}/{endpoint_slug}/cancel/{job_id}` | none found — verify by hand | — | gap | stops a running serverless job. cancel_instance covers GPU instances; nothing covers this. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| POST | `/v1/serverless/{endpoint_id}/{endpoint_slug}/openai/v1/chat/completions` | none found — verify by hand | — | redundant | an OpenAI-protocol shim over the same capability run_serverless_job already reaches. It exists so an OpenAI SDK can be pointed at the endpoint, which is a client-compatibility surface rather than a tool one. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| POST | `/v1/serverless/{endpoint_id}/{endpoint_slug}/openai/v1/embeddings` | none found — verify by hand | — | redundant | an OpenAI-protocol shim over the same capability run_serverless_job already reaches. It exists so an OpenAI SDK can be pointed at the endpoint, which is a client-compatibility surface rather than a tool one. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| GET | `/v1/serverless/{endpoint_id}/{endpoint_slug}/openai/v1/models` | none found — verify by hand | — | redundant | an OpenAI-protocol shim over the same capability run_serverless_job already reaches. It exists so an OpenAI SDK can be pointed at the endpoint, which is a client-compatibility surface rather than a tool one. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| POST | `/v1/serverless/{endpoint_id}/{endpoint_slug}/run` | none found — verify by hand | — | covered | run_serverless_job calls this path. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| POST | `/v1/serverless/{endpoint_id}/{endpoint_slug}/runsync` | none found — verify by hand | — | gap | the synchronous variant of run: it blocks for the result instead of returning a job id. Different behaviour, not a second spelling, and no tool reaches it. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| GET | `/v1/serverless/{endpoint_id}/{endpoint_slug}/status/{job_id}` | none found — verify by hand | — | covered | get_serverless_job_status calls this path. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |
| GET | `/v1/serverless/{endpoint_id}/{endpoint_slug}/stream/{job_id}` | none found — verify by hand | — | gap | streams a job's output as it is produced; no tool reaches it, so an agent can only poll for completion. The `{endpoint_slug}` form is the same handler under a second decorator — a vanity path, not a second endpoint — so it carries its twin's classification by construction. |

## `routes/sla.py` — 6 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/sla/downtimes` | _require_scope, _get_current_user | Get all currently-open downtime periods across all hosts. | gap | P5 SLA-aware placement needs these as tools |
| POST | `/api/sla/enforce` | _require_scope, _require_admin | Run monthly SLA enforcement for a host. | gap | P5 SLA-aware placement needs these as tools |
| GET | `/api/sla/hosts-summary` | _require_scope, _get_current_user | Get SLA status summary for all known hosts. | gap | P5 SLA-aware placement needs these as tools |
| GET | `/api/sla/targets` | none found — verify by hand | Get SLA target definitions for all tiers. | gap | P5 SLA-aware placement needs these as tools |
| GET | `/api/sla/violations/{host_id}` | _require_scope, _get_current_user | Get SLA violation history for a host. | gap | P5 SLA-aware placement needs these as tools |
| GET | `/api/sla/{host_id}` | _require_scope, _get_current_user | Get SLA record and rolling uptime for a host. | gap | P5 SLA-aware placement needs these as tools |

## `routes/spot.py` — 5 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/pricing/spot-enabled` | none found — verify by hand | Spot instance feature flag — global kill switch status for UI. | gap | P5 placement and migration need spot data as tools |
| GET | `/api/pricing/spot-floor-suggestion` | none found — verify by hand | Suggested provider spot floor and effective rate preview for a GPU model. | gap | P5 placement and migration need spot data as tools |
| GET | `/spot-prices` | none found — verify by hand | Get current unified spot prices for all GPU models. | gap | P5 placement and migration need spot data as tools |
| POST | `/spot-prices/update` | _require_admin | Trigger spot price recalculation. | gap | P5 placement and migration need spot data as tools |
| POST | `/spot/preemption-cycle` | _require_admin | Capacity preemption cycle for ops (set ``dry_run=true`` to preview only). | gap | P5 placement and migration need spot data as tools |

## `routes/ssh.py` — 3 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/ssh/keys` | _require_scope, _require_auth | List the authenticated user's SSH public keys. | gap | No tool lists keys. Deliberate: Quick Connect holds ssh:write and not ssh:read, so granting a listing tool would widen the connector's read surface for no capability it needs today |
| POST | `/api/ssh/keys` | _require_scope, _require_auth | Upload a user SSH public key. Like GitHub/AWS key management. | covered | register_ssh_key (mcp/src/tools/compute.ts), ssh:write. Reachable by a client_credentials token since the route stopped guarding with _require_user_grant; asserted by tests/test_agent_can_register_its_own_key.py |
| DELETE | `/api/ssh/keys/{key_id}` | _require_scope, _require_auth | Delete a user SSH public key by ID. | gap | No tool removes a key. Revocation is the operation most likely to be wanted under pressure, so it needs a confirm-gated tool rather than arriving as a side effect of the registration one |

## `routes/stripe_connect_v2.py` — 11 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| GET | `/api/connect/accounts` | none found — verify by hand | List all connected accounts stored in our database. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/connect/accounts` | none found — verify by hand | Create a new Stripe connected account using the V2 API. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| GET | `/api/connect/accounts/{account_id}/onboarding-link` | none found — verify by hand | Generate a Stripe Account Link for onboarding a connected account. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| GET | `/api/connect/accounts/{account_id}/status` | none found — verify by hand | Retrieve the current onboarding / requirements status of an account. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/connect/checkout` | none found — verify by hand | Create a Stripe Checkout Session using a *destination charge*. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| GET | `/api/connect/products` | none found — verify by hand | List all products, optionally filtered by connected-account ID. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/connect/products` | none found — verify by hand | Create a Stripe Product at the *platform* level. | gap | P6's provider surface — register, onboard, earnings, payout, Connect accounts; no tool reaches any of it |
| POST | `/api/connect/webhooks` | none found — verify by hand | Handle Stripe thin webhook events for V2 account changes. | internal | processor webhook; machine-to-machine and signature-verified, and P1's only completion signal — the caller is Stripe, never an agent |
| GET | `/connect/dashboard` | none found — verify by hand | Serve the Connect Dashboard HTML page. | internal | server-rendered browser page, not an API surface; the provider reads it during onboarding |
| GET | `/connect/storefront` | none found — verify by hand | Serve the Storefront HTML page. | internal | server-rendered browser page, not an API surface; the provider reads it during onboarding |
| GET | `/connect/success` | none found — verify by hand | Serve the payment success page after a completed checkout. | internal | server-rendered browser page, not an API surface; the provider reads it during onboarding |

## `routes/teams.py` — 10 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/teams` | _require_scope | Create a new team/organization. Creator becomes team admin. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| PATCH | `/api/teams/active` | none found — verify by hand | Set the caller's active team context (wallet, concurrency, job scope). | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| GET | `/api/teams/invite/{token}` | none found — verify by hand | Accept a team invitation. Adds the user to the team if their account exists. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| POST | `/api/teams/invite/{token}/accept` | none found — verify by hand | Accept invite for a logged-in user (completes after registration or sign-in). | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| GET | `/api/teams/me` | none found — verify by hand | Get teams the current user belongs to. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| DELETE | `/api/teams/{team_id}` | none found — verify by hand | Delete a team. Only the team owner can delete it. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| GET | `/api/teams/{team_id}` | none found — verify by hand | Get team details including members. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| POST | `/api/teams/{team_id}/members` | none found — verify by hand | Add a member to a team. Only team admins can add members. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| DELETE | `/api/teams/{team_id}/members/{email}` | none found — verify by hand | Remove a member from a team. Admins can remove anyone; members can leave. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |
| PATCH | `/api/teams/{team_id}/members/{email}` | none found — verify by hand | Update a team member's role. Only admins can change roles. | internal | team and member administration, an account-console surface; no P0-P7 journey creates a team or changes a membership |

## `routes/terminal.py` — 1 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/terminal/ticket` | _require_scope, _require_auth | Issue a short-lived one-time WebSocket ticket for terminal access. | gap | P2's terminal ticket. Scoped for instances:connect in 9c9bb5a; still no tool mints one, so connecting remains a browser or raw-HTTP act |

## `routes/transparency.py` — 3 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/transparency/legal-request` | _require_scope, _require_auth | Record a legal request (subpoena, warrant, MLAT, etc.). | internal | legal-request handling and the published transparency report. `transparency:read`/`write` are operator scopes and only a platform admin may be granted them |
| POST | `/api/transparency/legal-request/{request_id}/respond` | _require_scope, _require_auth | Record response to a legal request. | internal | legal-request handling and the published transparency report. `transparency:read`/`write` are operator scopes and only a platform admin may be granted them |
| GET | `/api/transparency/report` | _require_scope, _require_auth | Generate transparency report — CLOUD Act diligence artifact. | internal | legal-request handling and the published transparency report. `transparency:read`/`write` are operator scopes and only a platform admin may be granted them |

## `routes/verification.py` — 6 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/agent/verify` | none found — verify by hand | Receive comprehensive benchmark report and run verification checks. | internal | worker-agent callback posting verification evidence; authenticated as the fleet, never an agent |
| GET | `/api/verified-hosts` | none found — verify by hand | List all verified hosts with full verification details. | gap | which hosts carry verification. P5's placement preference is stated as 'prefer verified hosts above 99.5% uptime', so this is an input that phase needs and no tool exposes |
| POST | `/api/verify/{host_id}` | _require_scope, _get_current_user | Run verification checks on a host. | gap | a provider submitting their host for verification — P6's admission evidence step; no tool reaches it |
| POST | `/api/verify/{host_id}/approve` | _require_admin | Admin manually approves a host, overriding automated checks. | internal | platform decision on a verification submission; operator only |
| POST | `/api/verify/{host_id}/reject` | _require_admin | Admin manually rejects/deverifies a host. | internal | platform decision on a verification submission; operator only |
| GET | `/api/verify/{host_id}/status` | none found — verify by hand | Get current verification status for a host. | gap | the provider checking where their verification stands; P6, no tool |

## `routes/volumes.py` — 14 operations

| Method | Path | Auth dependency | Summary | class | notes |
|---|---|---|---|---|---|
| POST | `/api/v2/admin/volumes/reopen-encrypted` | _require_scope, _get_current_user | Reopen all encrypted volumes after NFS server reboot. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| GET | `/api/v2/volumes` | _require_scope, _get_current_user | List volumes owned by the current user. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| POST | `/api/v2/volumes` | _require_scope, _get_current_user | Create a new persistent volume. Billed in real-time from credits. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| GET | `/api/v2/volumes/available` | _require_scope, _get_current_user | List volumes available for attachment (status=available) for the current user. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| DELETE | `/api/v2/volumes/{volume_id}` | _require_scope, _get_current_user | Delete a volume. Must not have active attachments. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| GET | `/api/v2/volumes/{volume_id}` | _require_scope, _get_current_user | Get volume details. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| PATCH | `/api/v2/volumes/{volume_id}` | _require_scope, _get_current_user | Rename a volume. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| POST | `/api/v2/volumes/{volume_id}/attach` | _require_scope, _get_current_user | Attach a volume to a running instance. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| POST | `/api/v2/volumes/{volume_id}/detach` | _require_scope, _get_current_user | Detach a volume from its current instance. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| POST | `/api/v2/volumes/{volume_id}/retry` | _require_scope, _get_current_user | Retry provisioning for a volume stuck in 'error' status. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| GET | `/api/v2/volumes/{volume_id}/snapshots` | _require_scope, _get_current_user | — | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| POST | `/api/v2/volumes/{volume_id}/snapshots` | _require_scope, _get_current_user | Take an instant CoW snapshot of a detached volume. | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| DELETE | `/api/v2/volumes/{volume_id}/snapshots/{snapshot_id}` | _require_scope, _get_current_user | — | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |
| POST | `/api/v2/volumes/{volume_id}/snapshots/{snapshot_id}/restore` | _require_scope, _get_current_user | — | gap | P3 promotes artifacts to volumes; none of it is reachable as a tool |

