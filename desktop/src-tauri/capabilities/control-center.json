{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "control-center",
  "description": "Capability set for the bundled native control center window.",
  "windows": ["control-center"],
  "permissions": [
    "core:default",
    "default"
  ]
}
